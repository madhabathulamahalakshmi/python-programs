num = int(input("Enter a number: "))
temp = num
digits = len(str(num))
total = 0

while temp > 0:
    total += (temp % 10) ** digits
    temp //= 10

if total == num:
    print("Armstrong Number")
else:
    print("Not an Armstrong Number")
